// Bminor Syntax highlighting
// cpp comment

/* one line c */

/*
	Multiline
	C
	Comment
*/


function integer main() = {
	z: string = "hello";
	b: boolean = true;
	x: integer  = 0;
	c: char = 'x';

	x = x * 3 + 2 - 1;

	for(x; x < 10; x++){
		if ( (b && true) || (b && false) ){
			print c, x, '\n';
		}
	}

	if(z == "world"){
		print "hello ", z, '\n';
	} else if(z == "hello"){
		print z, " world", '\n';
	}

	return 0;
}
